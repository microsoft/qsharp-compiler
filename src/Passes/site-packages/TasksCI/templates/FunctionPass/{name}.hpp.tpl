#pragma once
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

#include "Llvm.hpp"

namespace microsoft
{
namespace quantum
{

class {name}Pass : public llvm::PassInfoMixin<{name}Pass>
{
public:
  /// Constructors and destructors
  /// @{
  {name}Pass()                    = default;
  {name}Pass({name}Pass const &) = default;
  {name}Pass({name}Pass &&)      = default;
  ~{name}Pass()                   = default;  
  /// @}

  /// Operators
  /// @{
  {name}Pass &operator=({name}Pass const &) = default;
  {name}Pass &operator=({name}Pass &&) = default;
  /// @}

  /// Functions required by LLVM 
  /// @{
  llvm::PreservedAnalyses run(llvm::Function &function, llvm::FunctionAnalysisManager &fam);
  static bool             isRequired();
  /// @}
};

} // namespace quantum
} // namespace microsoft
