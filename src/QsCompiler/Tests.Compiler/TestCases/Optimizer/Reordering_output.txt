namespace Microsoft.Quantum.Testing.Optimization.Reordering {
    Operation Test (x) : UnitType {
        QsBody (x) {
            let y = (LocalVariable x + IntLiteral 2L);
            let z = (LocalVariable y + IntLiteral 2L);
            (GlobalCallable f of LocalVariable y);
            (GlobalCallable f of LocalVariable z);
        }
    }

    Operation f (n) : UnitType {
        QsBody Intrinsic
    }
}